"""The officials — agents that staff the three branches.

Phase 0 ships deterministic *stub* agents (no LLM). Later phases swap in real
LLM-backed agents behind the same interfaces defined in ``base``.
"""
