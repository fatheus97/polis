version: 2
updates:
  # Polis's core is standard-library only (no pip/npm/docker app dependencies), so the
  # only thing to keep current is the GitHub Actions used by the workflows.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    groups:
      actions-minor-patch:
        update-types: [minor, patch]
